# pnpm 11 reads per-project settings here (the package.json "pnpm" field is
# ignored). Standard keys, understood by vanilla pnpm via corepack, so this
# file is safe to include in the Docker build context.
onlyBuiltDependencies:
  - esbuild
ignoredBuiltDependencies:
  - '@prisma/engines'
  - prisma
